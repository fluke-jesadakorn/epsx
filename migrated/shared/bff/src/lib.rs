//! Shared BFF utilities: security middleware, JWT verification, common
//! route builders. Used by both `apps/frontend` and `apps/admin`.

pub mod middleware;
pub mod auth_helpers;
pub mod static_assets;
