'use client';

import { NotFoundContent } from '@/components/shared';

/**
 * Not Found Page (404)
 * Uses unified status page component for consistent design
 */
export default function NotFound() {
  return <NotFoundContent />;
}
