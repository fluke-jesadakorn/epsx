apiVersion: v1
kind: Service
metadata:
  name: epsx-analytics
  labels:
    app: epsx-analytics
spec:
  selector:
    app: epsx-analytics
  ports:
    - name: http
      port: 8080
      targetPort: 8080
