# A7.0 frontend live-data and interaction audit

Status: **integrity PASS target; production readiness STOP**. This is an evidence and execution contract only. It does not authorize frontend/runtime changes, backend policy duplication, production access, or deployment. The machine-readable source of truth is [`contracts/frontend-live-data.json`](contracts/frontend-live-data.json).

## Baseline and audit result

The source baseline is pinned to `origin/development@373bd231cb7a616c3d4c0ddc1d60e0099a88a5db`. The route set is the checked 28-route frontend application in [`contracts/routes.json`](contracts/routes.json); the gate rejects missing, additional, duplicated, or reassigned routes.

The current result is deliberately not a completion claim:

- **1 aligned:** `/about`, after focused Rust and real localhost Playwright proof for content/escaping, semantics, responsive layout, and keyboard navigation.
- **10 partial:** `/access-denied`, `/contact`, `/developer/docs`, `/manual`, `/news`, `/news/:slug`, `/notifications`, `/offline`, `/privacy`, `/terms`.
- **17 blocked:** every other route in the contract.

The former B7.1 `/access-denied` alignment is superseded by a safety-driven source drift. The public page now ignores the pinned free-form `reason` and `route` query fields, renders one fixed generic denial reason, emits no query-derived permission claim, and retains only safe Home and Contact navigation. Its focused Playwright source now expects that behavior, but no current browser rerun has occurred. The route remains **partial** until product accepts the generic safety drift or a server-owned typed non-query denial context can restore truthful contextual detail. The focused B7.2 proof gives `/offline` a public accessible retry button and a fail-closed service-worker contract. After installation from another online page, a newly controlled mobile or desktop navigation can load `/offline` while disconnected and retry by keyboard after reconnection. CacheStorage contains exactly `GET /offline` in `epsx-public-offline-v1`; API, auth, account, profile, notification, analytics, admin, payment, user-query, and query-bearing `/offline` requests remain network-only and add no entry. The route remains **partial** because its truthful public-shell-only capability copy necessarily differs from the pinned source's unsupported claims that notification, analytics, and settings data are cached and will sync. `/privacy` has responsive, hierarchy, and keyboard proof, yet its Google Sign-in/OIDC legal copy is not approved for the canonical wallet/SIWE flow. `/terms` retains its static/unit hierarchy contract and native privacy/contact links, but its updated responsive/keyboard browser fixture has not been rerun after intentionally omitting the pinned newsletter form: `/api/public/subscribe` has no matching email-subscription handler, and no validation, rate limiting, or pending/success/error/retry contract exists. Terms therefore remains **partial** for legal approval, accepted source drift, or a future real endpoint with complete feedback.

The focused B1 `/about` proof removes the prior invented team biographies, company statistics, roadmap, values, and hiring claims. It accepts only the pinned hero, DataTech lifecycle, benefits, mission, and vision copy in the pinned order, carries the exact title/description/keywords through an escaped optional metadata field, and proves the middleware-required route through an ephemeral localhost RS256/JWKS session. The fixture binds only loopback, uses a five-minute token and temporary key, and stops both processes after the mobile/desktop run; it adds no authentication bypass.

The focused B1 `/news` and `/news/:slug` slice removes the frontend-owned article catalog, list fallback, synthesized detail fallback, and hard-coded related articles. Both SSR and JSON BFF paths now use the same strict content-service adapter and tagged ready/empty/not-found/error outcomes. Canonical envelopes reject any non-empty error and unknown outer/data/article fields; the current raw legacy list/detail forms must match their exact allowlisted shapes, so error-bearing payloads cannot become empty/200. The list bounds `q` and `page`, accepts only the four rendered `all`/`updates`/`engineering`/`product` categories, rejects the removed public `limit`, fixes display pages at the pinned 12 items, preserves `q`/category through native GET pagination, and gives an out-of-range empty page a keyboard-accessible filter-preserving Previous link. List/detail dates are validated and normalized to deterministic en-US display text. Detail loading is gated by the exact lowercase slug grammar: trailing slashes, extra segments, encoded values, and malformed slugs retain generic 404 classification instead of becoming dependency 503. It rejects mismatched slugs, propagates upstream 404, derives live metadata, context-escapes title and summary before the unescaped outer shell, returns 404/503 truthfully from SSR, and renders article body as escaped text rather than injecting upstream HTML. Hermetic localhost adapter tests, a full-shell metadata injection test, and focused Rust render tests cover these boundaries. Both routes remain **partial**: A5 has not frozen the canonical envelope/status/query/category/date/body-format/correlation/timeout/retry contract; the list adapter can see only the current content service's first 100 records before local filtering/pagination; the current content detail handler returns rendered HTML while the pinned source expects Markdown and itself synthesizes a matching coming-soon article for unknown slugs, so full GFM presentation and truthful end-to-end 404 remain blocked; and no live-service browser runtime proof exists yet.

The focused B1 Home news slice reuses that strict normalized public list outcome for exact `/` and the existing `/index` alias. It always uses the adapter's default page/query/category, ignores Home query fields, and stores the result under a distinct `data_home_news` parameter so Home cannot select or corrupt `/news` route state. Ready renders the first one to three validated rows in source order as one lead card plus up to two smaller cards; a `featured` badge follows only the row flag and never promotes or reorders a later article. Empty, dependency error, missing, and malformed outcomes remain distinct, all article text is escaped, and recovery uses native news and retry-home links without cached or sample articles. Home remains **blocked**: market and plan previews are still unavailable under A4/A5, and this is a general-list preview rather than parity with the pinned dedicated featured endpoint. It inherits the adapter's first-100/fixed-12 boundary before taking three rows, cannot distinguish the content service's missing-file empty fallback from genuine empty content, does not reproduce the pinned 60-second ISR policy, and has no live-service/browser proof.

The focused B4 `/notifications` slice is now a truthful read-only partial. Authenticated SSR records explicit success or dependency error, and the page consumes the current notification-service DTO without sample fallback. Required nullable keys must be present, timestamps deserialize as UTC datetimes, missing/malformed/failed payloads remain distinct from an empty list, absent/blank priority remains absent, read state comes only from `read_at`, counts are labelled as loaded-list counts, content is escaped, and unapproved action URLs are parsed but never rendered. Present raw priority text is never normalized or relabelled: an exact static presentation-only map gives `critical`/`urgent`, `high`, `normal`, and `low` compact red, orange, blue, and green chips respectively, with every other present value receiving the neutral chip. Color is supplementary to the escaped visible label and screen-reader-only `Priority:` prefix. Deterministic emitted-CSS tests prove the priority-chip light/dark foreground/background pairs, the loaded-list count, and timestamps meet the small-text `>= 4.5:1` threshold, while the filled unread dot exceeds the non-text `>= 3:1` threshold across the declared `.page-bg-app`, glass-card, read/unread, and hover surface envelope; the same tests bind every computed surface to its emitted gradient, overlay, and row declaration. Read rows are no longer diluted by inherited row opacity. Static SSR and emitted-CSS tests also prove that complete escaped hostile title/body head and tail stay in their semantic `h3`/`p` and normal wrapping flow without ellipsis, nowrap, line clamps, display-box clipping, or hidden overflow. That is only full-DOM/static-CSS visibility evidence, not browser/mobile layout proof: the BFF has a 2 MiB whole-response cap across up to 100 rows but no per-field title/subject/body bounds, so per-field layout safety is not claimed. Deterministic boundary and row tests also prove that every `created_at` later than the shared server render instant receives a visible absolute UTC label including seconds while its canonical `datetime` and exact UTC title remain unchanged. That label is one server-rendered snapshot with no browser live updating and makes no lifecycle inference. Pinned `?id=` highlight, scroll, and read continuity remains deferred: no pinned producer emits that query, current SSR loads only the default first 50 rows, and a static highlight would neither scroll nor distinguish unloaded, invalid, or foreign identifiers while adding a first-page membership signal. The hollow/read dot is an unclaimed redundant cue, separators remain decorative and `aria-hidden`, remaining adjacent metadata, browser/runtime rendering, and whole-page AA remain unproven. The obsolete frontend-only `notifications:read` gate and the prior local filters, mark/delete/bulk, preference, browser-permission, push, and action controls are absent; the static SSR list needs no page-local hydration and recovery is a native retry link.

The active shared header now has a narrower browser-only read controller. Its badge begins empty, hidden, and unavailable; only a server-verified authenticated non-offline response receives the controller, so signed-out pages have no notification runtime or fetch path and the public-cacheable `/offline` response excludes user-specific activity. Authenticated non-offline HTML and every notification list/unread BFF outcome carry `private, no-store`, and the fetch itself requests `no-store`. It calls exact `GET /api/v1/notifications/unread-count` with credentials, accepts only a sole non-negative safe-integer `count`, ignores stale success and catch paths with a generation guard, hides zero/error/malformed outcomes, caps only visible text at `99+`, exposes the exact count through the link label, uses AA-contrast `#dc2626` behind the small white text, and uses `textContent` without notification mutations or HTML injection. This is static/unit evidence only. It does not prove source-compatible pagination, unread-count envelope/broadcast/expiry/read semantics, mutations, preferences, SSE/push, action-URL policy, live-service behavior, or browser runtime behavior, so the route remains **partial** under A1/A5 and all A11 lifecycle blockers remain open.

The focused B2 `/account` owner-history slice restores the pinned source's server-prefetched first page without treating absence or failure as an empty ledger. A locally verified session is the only source of the wallet path, the request is fixed to `limit=10&offset=0`, the gateway admits only exact authenticated `GET /api/v1/pay/history/{safe-wallet}`, and the Pay service repeats supported-audience authentication plus `canonical_owner` before its owner-scoped queries. The legacy backend proxy no longer writes dynamic paths, query strings, upstream URLs, wallet addresses, or caller-supplied identifiers into its warning. The frontend accepts only the complete Pay wire shape: response and every row must match the verified owner, totals must be non-negative and consistent, each collection is bounded to ten rows, required text is bounded/control-free, timestamps parse, and empty is valid only when both collections and both totals are zero. The page renders distinct signed-out, ready, empty, unavailable, and malformed outcomes with native sign-in, refresh, and retry links, token/amount, paid/received direction, status, counts, and semantic timestamps. The subsequent truthfulness slice removes the canned `/api/v1/account` BFF producer, ambiguous identity profile fetch, `data_account` parsing, `$0`/Join Now/Web3 Vault and Active/Secure fallbacks, locally toggled notification defaults, and fake persistence success. Only the locally verified session wallet and known authentication method render; member date, credits, access/plans, and preferences are explicit signed-out or unavailable source-like states with native navigation. `/account` remains **blocked**: isolated database owner-separation and browser proof, later payment pagination, and strict owner profile/member-date, credit, access-plan projection, and notification-preference contracts are absent.

The focused B2 `/profile` truthfulness slice removes the frontend-invented `profile:read`/`profile:write` authorization gate and all fabricated connected-wallet, provider, address, tier, role, email-verification, permission, API-key, preference, export, and deletion state. Its authentication-only page renders only the subject, wallet, authentication method, and exact backend-issued permissions carried by the locally verified access token. Four exact-query native links select Web3, Account, Email, and Data panels; unknown, encoded, or duplicate tab values fail closed to Web3. Unsupported workflows are read-only unavailable notices with no form, input, button, or local success transition. `/profile` remains **blocked**: JWT/JWKS verification failures still collapse into the unauthenticated redirect rather than a typed loading/error/retry outcome, and A1 runtime/browser proof plus canonical owner profile, wallet-connection, email, API-key, preference, export, and deletion contracts under A4/A5 are absent.

The focused B2 `/account/credits` truthfulness slice removes the canned `$250/$1,250/$1,000` BFF response, dead `/api/v1/credits` fetch, permissive partial DTO, `f64` amounts, failure-as-zero balance, failure-as-empty ledger, invented transaction rows, and inert filter signals. Signed-out users receive a native return-to-page sign-in link; authenticated users see the source-like three-card/history layout with every value explicitly unavailable, a verified-session wallet label, and native account navigation. The route remains **blocked** because A6 has not selected financial authority: the retained monolith balance read mutates by creating a row, history filters are unbounded, totals are not authoritative, the gateway and Pay service expose no credit read, and isolated owner/browser proof is absent.

The focused B3 `/analytics` truthfulness slice removes the public canned analytics BFF producer, dead summary fetch, fixed ranking cards/metrics/events/P&L/volume, fake `analytics:read` gate, and local filter/export/search/pagination/watchlist controls. The source's public route boundary remains: signed-out and signed-in visitors both receive an accessible source-like unavailable surface, with authentication changing only native sign-in/account navigation. Compatibility payloads are ignored and no plan, permission, ranking offset, or watchlist ownership is inferred. The route remains **blocked** until A4/A5 provide a strict rankings DTO and backend-owned access/offset decisions, bounded query/filter/sort/pagination, owner watchlists, export policy, complete async outcomes, and browser proof.

The focused B5 `/developer` truthfulness slice removes the canned developer and documentation BFF producers, dead developer loader, plaintext production-looking keys, fixed plan/rate/usage/expiry claims, default permission selection, and local create/revoke/refresh/expiry controls. The authentication-only boundary remains; authenticated users receive an accessible unavailable surface with native documentation navigation, while payload fixtures and secrets are ignored. The route remains **blocked** until A1/A4/A5 provide authenticated owner-scoped API-key list/create/revoke, backend-authorized scopes/plans, secret-once delivery, rate/usage/expiry reads, audit/recovery, complete async outcomes, and browser proof.

The focused B5 `/developer/usage` truthfulness slice removes its canned in-process producer, wire fixtures, sample metrics, charts, key rows, history, rate/error/uptime claims, and simulated wallet picker. The existing authentication-only gate remains; an authenticated request receives an accessible unavailable state with native documentation navigation. The route remains **blocked** until A4/A5 provide owner-scoped metering, backend-owned plan/rate decisions, strict DTO and range/pagination contracts, typed loading/error outcomes, and owner/browser proof.

The focused B6 payment truthfulness slice replaces both `/payment` routes with one fail-closed unavailable surface. It removes raw-query redirect/inline-script construction, query-owned amount/token/wizard state, fixed `29.00`/`USDT` and access fixtures, the confirm POST, success/submitted claims, dead canned intent lookup, and both invented `payments:read` UI gates. The dynamic route accepts only bounded ASCII/control-free type/reference labels, escapes them, and explicitly says they do not confirm an intent. Both routes remain **blocked** until A6 proves owner-safe intent, verified price/asset/recipient, wallet submission, idempotency, status/finality, audit, recovery, and browser behavior.

The focused B6 `/contact` accessibility/controller slice retains the native `mailto:info@epsx.io` action and gives the semantic copy-email button a stable accessible name and visible `Copy` label. The button is explicitly associated with an initially empty `role="status"` region using `aria-live="polite"` and `aria-atomic="true"`; success and failure write the exact `Email address copied to clipboard.` and `Could not copy email address.` announcements through `textContent` without relabelling the button. Clipboard rejection, synchronous failure, and an absent Clipboard API fall back to `execCommand`; generation and timer guards suppress stale async results and resets; and the temporary textarea path restores focus and cleans up safely even when DOM operations throw. The shared generic copy-button flash behavior remains intact, and complete inline `onclick` expressions are escaped for their outer HTML attribute context. Focused Rust tests plus a hermetic Node VM controller proof cover those contracts only. No browser, assistive-technology, or middleware-authenticated responsive rerun occurred, so `/contact` remains **partial** until those real-runtime keyboard, announcement, layout, and auth-boundary checks pass.

The focused B5 `/manual` proof preserves the pinned 35-feature descriptions, screenshots, and catalog fingerprint as intended-workflow reference material. A deterministic generated Rust module is byte-compared against the ordered 28-route JSON contract; comments, helper logic, string decoys, or any other extra bytes fail the verifier. A comment/string-aware literal parser independently requires 35 feature cards, 21 exact normalized routes, the pinned duplicate-route multiplicities, zero unmapped cards, and only the exact `/payment/[type]/[id]` normalization. Across the 35 cards it renders 1 `Migration aligned`, 10 `Migration partial`, and 24 `Migration blocked` badges. These labels expose raw route readiness only and never claim feature or runtime availability. Every concrete card uses the neutral `View route` action regardless of readiness, unknown routes fail closed, and the exact payment template is a noninteractive `Route template only` status. The badges are migration evidence, not runtime, authentication, permission, plan, entitlement, ranking, subscription, or feature-flag decisions. Focused source, Rust, and verifier proof covers the new drift prevention; no new browser rerun or product acceptance occurred. Keep the route partial until product accepts the safety-driven source drift, capability descriptions are synchronized with authoritative current state, and the visible notice, badges, actions, unknown-route fallback, and dynamic-template behavior receive browser and assistive-technology proof.

The focused B5 `/developer/docs` proof removes the invented page header and unused canned loader, retains the pinned four-category/ten-endpoint inventory, endpoint order, methods, paths, tables, rate references, examples, and inherited root metadata, and restores the mobile section navigator, accessible endpoint accordions, language tabs, and code/response copy controls through a constant route-scoped controller. The mobile navigator intentionally starts closed instead of preserving the source hook's obstructive `sidebarOpen = true` first paint; it exposes an accessible toggle, overlay, Escape close, and focus restoration. The prior authenticated localhost proof covers the preserved mobile/desktop layout, navigator, accordions, tabs, copy controls, and keyboard paths; it does not prove the changed page copy. A prominent accessible note now states that endpoint, authentication, tier, rate, schema, and example content is a pinned migration reference rather than a verified production contract and warns against real credentials. The hero, authentication, sidebar, and request-availability prose intentionally drifts from the source for safety. All example origins are non-routable `https://api.example.invalid`; each former disabled Try It panel is now a noninteractive `Live requests unavailable` note with no credential field, request control, or executor. This safety-driven source change has focused Rust proof but no current browser rerun. The route remains **partial** while A1 has not resolved the pinned authenticated DeveloperLayout versus the target's existing public BFF boundary and A4/A5 have not locked the advertised tier/rate/route contract.

The B7.2 worker is deliberately narrower than a general PWA cache. `GET /service-worker.js` is a constant public response; install fetches exact `/offline` with `credentials: 'omit'`, requires the same-origin HTML response and server-owned `x-epsx-public-cache: offline-shell-v1` marker, and writes only that key. Fetch interception applies only to a same-origin, query-free, `GET` navigation whose path is exactly `/offline`. It never caches the current page, static business payloads, API responses, cookies, authentication/session material, or owner/admin/payment data. A browser with no previously installed worker still cannot navigate offline; the proof is specifically a fresh navigation by a newly controlled client after online installation.

The focused home, plan, and chat truthfulness slice removes fixed market performers, numeric/live claims, plan catalogs/prices/promotions/features/eligibility, fixed news previews, sample conversations/messages/presence/counts, and every local-only filter/create/send/resolve/checkout action. Home now consumes a strict public-news outcome while its market and plan sections remain explicitly unavailable; `/plans`, `/chat`, `/chat/:id`, and `/chat/history` keep polished unavailable states with useful contact/home/inbox navigation. The dynamic chat reference is bounded, control-free, escaped, labeled unverified, and never treated as an owned conversation. The ranking and plan compatibility BFF producers plus the unused `/plans` SSR loader are absent. These routes remain blocked until backend-owned ranking, subscription, featured-news ordering, and verified-owner chat contracts have strict DTOs, complete async states, and isolated browser proof; payment mutation remains separately stopped under A6.

The intentional unavailable/no-loader routes `/analytics`, `/dashboard`, `/portfolio`, `/permissions`, `/chat`, `/chat/:id`, `/chat/history`, `/account/credits`, `/developer`, `/developer/usage`, and `/plans` no longer expose same-URL `Retry` or `Check again` links. Their retry state is **not-applicable** because no producer exists for a reload to recover; useful sign-in, account, profile, plans, contact, home, inbox, and documentation navigation remains where each route supports it. Producer-backed retries for news, notifications, and account payment history remain. This target-and-contract correction does not add or claim browser proof.

The most important distinction is loader presence versus a production-usable flow. Plans, chat, dashboard, portfolio, permissions, account identity/statistics, credits, developer overview/usage, analytics, and both payment routes deliberately have no unowned business-data loader: they render only locally verified session claims, explicit unavailable states, or strict payment-history outcomes. Home now has one public strict news loader but no market or plan producer. The focused Home news, dedicated news, notification, and account payment-history slices consume explicit dependency outcomes without sample-backed financial/list fallbacks, but remain non-aligned for the producer-contract, ordering, database, pagination, cache, and runtime-proof gaps above. None of the fail-closed or partially loaded routes is live-data readiness.

## Exact route anchors and observed loader path

Anchors are literal substrings. Source anchors are verified with `git show` at the pinned commit; target and loader anchors are verified against the current worktree. The JSON contract contains the second source/target anchor, payload classification, auth/owner boundary, all interaction categories, four async states, hydration status, dependencies, and route blocker text.

| Route | Pinned source anchor | Current target anchor | Observed loader | State |
|---|---|---|---|---|
| `/` | `apps/frontend/app/page.tsx :: export default async function HomePage` | `pages/home.rs :: data-home-market-state` | strict default-query `GET /api/v1/content/news?page=1&limit=100` outcome, capped to three source-order rows; market/plans unavailable; featured ordering, missing-file empty ambiguity, ISR parity, and browser proof open | blocked |
| `/about` | `apps/frontend/app/about/page.tsx :: title: 'About Us - EPSX Analytics Platform'` | `pages/about.rs :: meta.title = TITLE.to_string();` | accepted pinned static content only; authenticated responsive/metadata/keyboard proof | aligned |
| `/access-denied` | `apps/frontend/app/access-denied/page.tsx :: export default function AccessDeniedPage` | `pages/access_denied.rs :: GENERIC_DENIAL_REASON` | fixed generic reason, no public query-derived denial/permission semantics, safe links; updated browser proof source not rerun | partial |
| `/account` | `apps/frontend/app/account/page.tsx :: export default async function AccountPage()` | `pages/account.rs :: let payment_history_load = payment_history_load(&ctx);` | verified session wallet/auth method plus strict owner `GET /api/v1/pay/history/{session-wallet}?limit=10&offset=0`; other sections unavailable; isolated DB/browser proof and later pagination open | blocked |
| `/account/credits` | `apps/frontend/app/account/credits/page.tsx :: export default function CreditsPage()` | `pages/account_credits.rs :: if let Some(owner) = owner {` | no loader; signed-out or verified-session unavailable state until A6 selects authority | blocked |
| `/analytics` | `apps/frontend/app/analytics/page.tsx :: export default async function AnalyticsPage` | `pages/analytics.rs :: AnalyticsUnavailableContent { signed_in }` | no loader; public fail-closed state until strict backend rankings/A4 decisions exist | blocked |
| `/auth` | `apps/frontend/app/auth/page.tsx :: <WalletConnectAuth` | `pages/auth_page.rs :: data_connect_wallet: Some(true)` | browser wallet auth bridge | blocked |
| `/chat` | `apps/frontend/app/chat/page.tsx :: export default async function ChatPage()` | `pages/chat.rs :: data-chat-state` | no owner loader; authenticated unavailable state without samples, presence, filters, or fake mutations | blocked |
| `/chat/:id` | `apps/frontend/app/chat/[id]/page.tsx :: useChatSSE({ enabled:` | `pages/chat_conversation.rs :: data-chat-conversation-state` | no owner loader; bounded escaped route label is explicitly unverified and never selects data | blocked |
| `/chat/history` | `apps/frontend/app/chat/history/page.tsx :: const [loading, setLoading] = useState(true)` | `pages/chat_history.rs :: data-chat-history-state` | no owner loader; authenticated unavailable state without sample history, counts, or filters | blocked |
| `/contact` | `apps/frontend/app/contact/page.tsx :: CopyEmailBtn` | `pages/contact.rs :: fn contact_copy_email_has_stable_accessible_status_contract()` | no form or data loader; native mailto plus stable-label semantic copy button, initially empty polite status region, exact outcome announcements, guarded clipboard fallback, and attribute escaping have focused Rust/Node proof only; browser/AT/authenticated responsive proof open | partial |
| `/dashboard` | `apps/frontend/app/dashboard/page.tsx :: getSessionFromWeb3()` | `pages/dashboard.rs :: data-dashboard-state` | no unowned loader; verified session identity plus unavailable state | blocked |
| `/developer` | `apps/frontend/app/developer/page.tsx :: APIKeyManager` | `pages/developer.rs :: DeveloperOverviewUnavailable {}` | no loader; authenticated unavailable state until owner API-key/plan contracts exist | blocked |
| `/developer/docs` | `apps/frontend/app/developer/docs/page.tsx :: <ApiDocs />` | `pages/developer.rs :: DEVELOPER_DOCS_SOURCE_BASELINE` | version-pinned static catalog; unused canned loader removed; live Try It fail-closed pending A1/A4/A5 | partial |
| `/developer/usage` | `apps/frontend/app/developer/usage/page.tsx :: DeveloperUsagePage` | `pages/developer.rs :: DeveloperUsageUnavailable {}` | no loader; authenticated unavailable state until owner-safe metering exists | blocked |
| `/manual` | `apps/frontend/app/manual/page.tsx :: Complete guide to all platform features` | `pages/manual.rs :: const FEATURES: + manual_route_statuses.rs` | pinned 35-card/21-route intended-workflow catalog; exact generated module yields 1 Migration aligned, 10 Migration partial, and 24 Migration blocked raw route-readiness badges; every concrete action says View route, unknown routes fail closed, and the exact payment template is noninteractive; source/Rust/verifier proof only for this change, no new browser rerun or product acceptance | partial |
| `/news` | `apps/frontend/app/news/page.tsx :: searchParams: Promise<{ page?: string }>` | `pages/news.rs :: enum NewsListOutcome` | strict `GET /api/v1/content/news?page=1&limit=100`; exact envelopes/errors/categories, fixed 12-item pages, deterministic dates, tagged ready/empty/error, URL-stable native filtering/pagination/recovery | partial |
| `/news/:slug` | `apps/frontend/app/news/[slug]/page.tsx :: generateMetadata` | `pages/news_detail.rs :: enum NewsDetailOutcome` | strict `GET /api/v1/content/news/{slug}`; exact route/slug/envelope/date rules, outer metadata escaping, tagged ready/not-found/error | partial |
| `/notifications` | `apps/frontend/app/notifications/page.tsx :: page: parseInt(params.page ?? '1')` | `pages/notifications.rs :: enum NotificationLoad` | explicit-outcome owner `GET /api/v1/notification/list` plus authenticated shared-header `GET /api/v1/notifications/unread-count`; strict list/count validation, private/no-store owner responses, hidden-unavailable AA badge, signed-out/offline exclusion, race guard, exact accessible count, no mutations/injection; source parity and live browser proof absent | partial |
| `/offline` | `apps/frontend/app/offline/page.tsx :: window.location.reload()` | `pages/offline.rs :: data-offline-reload` | exact public-shell cache, fresh controlled disconnected navigation, query/sensitive bypass, retry/reconnect, mobile/desktop, keyboard, and cleanup proof; truthful copy differs from unsafe pinned cache claims | partial |
| `/payment` | `apps/frontend/app/payment/page.tsx :: PaymentPage` | `pages/payment.rs :: PaymentUnavailablePage { ctx: ctx.clone(), route_context: None }` | no loader or mutation; fail-closed unavailable state | blocked |
| `/payment/:type/:id` | `apps/frontend/app/payment/[type]/[id]/page.tsx :: Fetch plans on server` | `pages/payment.rs :: bounded_route_context(ctx.params.get("id"))` | no intent lookup or mutation; bounded labels are explicitly unverified | blocked |
| `/permissions` | `apps/frontend/app/permissions/page.tsx :: usePermissionsPage({ base })` | `pages/permissions.rs :: data-permissions-state` | locally verified raw session claims only; canonical permission service unavailable | blocked |
| `/plans` | `apps/frontend/app/plans/page.tsx :: PlansPage` | `pages/plans.rs :: data-plans-state` | no loader or mutation; public unavailable state until subscription-owned plan contract exists | blocked |
| `/portfolio` | `apps/frontend/app/portfolio/page.tsx :: <RequireSignIn` | `pages/portfolio.rs :: data-portfolio-state` | no unowned loader; signed-out gate plus authenticated unavailable state | blocked |
| `/privacy` | `apps/frontend/app/privacy/page.tsx :: 1. Information We Collect` | `pages/privacy.rs :: const LAST_UPDATED:` | responsive h1/h2 landmarks and keyboard proof pass; wallet/SIWE legal approval open | partial |
| `/profile` | `apps/frontend/app/profile/page.tsx :: <WalletProfileClient wallet={session.user} />` | `pages/profile.rs :: let selected_tab = selected_profile_tab(&ctx.query);` | locally verified session claims only; unsupported owner workflows unavailable | blocked |
| `/terms` | `apps/frontend/app/terms/page.tsx :: fetch('/api/public/subscribe'` | `pages/terms.rs :: fn terms_preserves_accessible_legal_content_without_unsupported_subscription()` | no form or data loader; unsupported pinned newsletter action intentionally omitted while privacy/contact links remain | partial |

Target paths in the table are abbreviated for readability; the verifier locks the full path `shared/rust/dioxus_ui/src/pages/<file>`. Loader anchors are in `apps/frontend/src/ssr.rs` where applicable.

## State, interaction, ownership, and hydration findings

Every route record explicitly inventories:

- static/sample payloads and placeholder/skeleton-only surfaces;
- producer kind, backend routes/loaders, and whether the page actually consumes the payload;
- public/optional/required authentication and the resource owner key;
- forms, pagination, search, wallet, keyboard, and other controls, including “missing” as an explicit finding;
- loading, empty, error, and retry as `present`, `missing`, or `not-applicable`;
- browser hydration need and current implementation state.

The cross-cutting stop conditions are:

1. Sample or zero-value fallbacks commonly turn dependency failures into plausible-looking success pages.
2. SSR-only rendering strips ordinary Dioxus event closures unless a proven hydration or raw browser bridge exists. A `use_signal` or `onclick` in source is not interaction proof.
3. Authenticated views need a verified session and owner-scoped backend queries. Dynamic identifiers must not expose foreign resources; use the backend’s chosen non-leaking result.
4. Frontend forms must surface pending, validation, success, error, and retry states. Local vector/signal mutations are not durable mutations.
5. Pagination and search must be URL- or backend-contract driven so refresh, back/forward, keyboard submission, and SSR all preserve state.
6. Wallet and payment success must come from A1/A6 verified state, never a cookie, query amount, local step, or submitted transaction alone.

## Dependency boundary

- **A1:** session/wallet ownership, safe return targets, real signature flow, rotation, revocation, and logout. Auth, identity, chat, owner views, and checkout cannot close before it.
- **A4:** permission, plan eligibility, ranking offset, feature flags, and entitlement decisions. The frontend may render backend decisions but must not recreate them.
- **A5:** route prefix, body/envelope/status/error, cookie/bearer forwarding, correlation, timeout, and retry alignment. A page adapter is not stable until this contract is locked.
- **A6:** intent, wallet submission, chain receipt/finality, status polling, idempotency, escrow, and activation. `/plans` checkout and both payment routes remain stopped.

## Small executable batches

Close one batch at a time; do not mark a route aligned merely because another route in its batch passes.

1. **B1 public content:** `/`, `/about`, `/news`, `/news/:slug`. `/about` is aligned from its accepted pinned static source. `/news` and `/news/:slug` no longer have frontend sample fallbacks and have focused proof for URL search/pagination, escaping, validation, not-found, error, and retry rendering; keep them partial until A5 freezes the producer contract, the content service stops synthesizing unknown slugs and exposes canonical server-side querying beyond the current 100-record adapter boundary, and live-service browser proof passes. Home has removed fixed market, pricing, plan, and article fixtures plus numeric/live claims. Its news section now consumes the same strict default list outcome and renders at most three source-order rows with explicit empty/error recovery, while market and plans stay unavailable. Keep Home blocked until A4/A5 provide those producers, a canonical featured-news/order contract resolves the general-list mismatch and missing-file empty ambiguity, cache behavior is accepted, and browser proof passes.
2. **B2 identity:** `/auth`, `/account`, `/account/credits`, `/profile`. Public desktop, compact, and mobile-sheet Connect controls now use native same-origin anchors to `/auth`; the shared shell makes no provider-selection or demo-login claim, while the actual `/auth` browser bridge remains unchanged. `/account` now consumes a strict read-only first-page Pay owner-history response with explicit failure/empty separation and no financial mutations, renders only verified session wallet/auth method, and marks unsupported member date, credits, access/plans, and preferences unavailable without local mutation; keep it blocked until isolated owner A/B database proof, focused browser proof, later payment pagination, and strict owner contracts for those sections pass. `/profile` now exposes only locally verified read-only session claims through native exact-query tabs, with unsupported workflows explicitly unavailable; keep it blocked until A1 runtime/browser proof and canonical owner profile, wallet-connection, email, API-key, preference, export, and deletion contracts exist. `/account/credits` no longer fabricates a balance or ledger and remains explicitly unavailable until A6 selects a non-mutating, owner-safe authority with bounded history and proof.
3. **B3 insights:** `/analytics`, `/dashboard`, `/portfolio`, `/permissions`. All four routes now reject or ignore canned business data and fail closed. Analytics remains public; dashboard preserves signed-out navigation and exposes only verified session identity when authenticated; portfolio preserves its Require Sign In UX and otherwise shows no holdings or market claims; permissions shows escaped raw verified-session strings as explicitly non-canonical and removes its circular `permissions:read` gate. They remain blocked until strict A4/A5 owner, ranking/query, dashboard, portfolio/watchlist, permission/history, market-provenance, mutation, async-state, and browser contracts exist. Render backend decisions without policy duplication.
4. **B4 communication:** `/chat`, `/chat/:id`, `/chat/history`, `/notifications`. Notifications now has an authenticated, sample-free, explicit-outcome, read-only owner list plus a fail-closed authenticated shared-header unread badge and remains partial; lock its source-compatible list/count envelopes, pagination, broadcast/expiry/read semantics and prove browser behavior before adding backend-owned lifecycle/preference/push/SSE/action-URL features. All three chat routes now require authentication and fail closed without samples, presence, counts, local filters, or fake create/send/resolve actions. They remain blocked until strict non-enumerating owner list/detail/history APIs, bounded pagination, idempotent mutations, SSE ordering/reconnect/deduplication, complete async states, keyboard behavior, and isolated owner/browser proof exist.
5. **B5 developer:** `/developer`, `/developer/docs`, `/developer/usage`, `/manual`. `/developer` and `/developer/usage` now expose only authenticated unavailable/recovery states, with no canned keys, secrets, plans, mutations, or metering; keep them blocked until owner-scoped secret-once API-key management, metering, and A4 rate/plan decisions exist. `/manual` remains a pinned static 35-feature/21-route intended-workflow catalog but is partial: an exact generated Rust module exposes this JSON contract's raw route readiness as 1 `Migration aligned`, 10 `Migration partial`, and 24 `Migration blocked` cards. All concrete actions say `View route`, unknown routes fail closed, and the exact payment template is noninteractive. Neither descriptions nor badges make feature/runtime availability, authentication, permission, plan, entitlement, ranking, subscription, or feature decisions. The new policy has source/Rust/verifier proof only; product acceptance, status-aware description synchronization, and a new browser/assistive-technology rerun remain. `/developer/docs` retains the pinned static catalog, metadata, responsive structure, accordions, language tabs, examples, and copy controls, but now labels the entire catalog as unverified reference material, uses only `.invalid` origins, accepts no credentials, and exposes no live request executor. Keep it partial until A1 resolves its auth boundary and A4/A5 provide a drift-checked route/auth/rate/entitlement/schema contract plus new browser proof.
6. **B6 commerce/support:** `/plans`, `/payment`, `/payment/:type/:id`, `/contact`. Plans now has no canned catalog, price, promotion, feature, countdown, eligibility, checkout, BFF producer, or unused SSR loader; it fails closed until subscription and permission backends provide the versioned public-plan and access contract. Both payment routes also fail closed with no query-owned financial state, mutation, canned lookup, or completion claim; payment stays stopped until A6 is separately authorized and complete. Contact preserves the native mailto and semantic copy-email controls and removes the unsupported invented submit form. Focused Rust and hermetic Node VM proof cover its stable label/name, associated initially empty polite status, exact `textContent` announcements, Clipboard-to-`execCommand` fallback, stale-result/reset guards, cleanup/focus restoration, preserved generic copy behavior, and complete attribute escaping; keep it partial until middleware-authenticated responsive and real browser/assistive-technology keyboard/status proof pass.
7. **B7 policy/fallback:** `/access-denied`, `/offline`, `/privacy`, `/terms`. `/access-denied` is partial after intentionally replacing public query-derived denial and permission semantics with a fixed generic reason; product acceptance or a server-owned typed non-query denial context is required before contextual detail and alignment. `/offline` now has exact public-shell-only service-worker delivery and fresh controlled-navigation proof, but needs explicit acceptance of its safety-required copy drift from the unsupported pinned cached-feature claims. Obtain product/legal approval for wallet/SIWE privacy and terms copy. Terms intentionally omits the pinned dead newsletter form and remains partial until that source drift is accepted or a real email-subscription endpoint provides validation, rate limiting, pending/success/error/retry feedback, and browser proof.

For each route, add a focused Rust unit/adapter test and a browser fixture that covers the contract’s applicable state and interaction fields. After a batch is implemented, change only evidence-backed route fields and anchors; the readiness gate will continue to exit `3` until all 28 route statuses are `aligned` and their blocker arrays are empty.

## Acceptance commands

Audit integrity and deterministic report:

```sh
./scripts/migration/verify-frontend-live-data.sh --mode integrity
./scripts/migration/verify-frontend-live-data.sh --mode emit
./scripts/migration/test-frontend-live-data.sh
bunx playwright test e2e/frontend/policy-fallback-runtime.spec.ts --project=frontend --workers=1
./scripts/migration/run-offline-fresh-runtime-proof.sh
bunx playwright test e2e/frontend/manual-runtime.spec.ts --project=frontend --workers=1
./scripts/migration/run-about-runtime-proof.sh
./scripts/migration/run-developer-docs-runtime-proof.sh
cargo test --locked --offline -p epsx-frontend --bin bff-frontend news -- --nocapture
cargo test --locked --offline -p epsx-dioxus-ui --lib pages::news -- --nocapture
cargo test --locked --offline -p epsx-gateway --lib
cargo test --locked --offline -p epsx-pay-svc --lib
cargo test --locked --offline -p epsx-frontend --bin bff-frontend account_payment_history -- --nocapture
cargo test --locked --offline -p epsx-dioxus-ui --lib payment_history -- --nocapture
```

Readiness is expected to stop today:

```sh
./scripts/migration/verify-frontend-live-data.sh --mode readiness
# expected exit 3 while any route is partial or blocked
```

Run the repository gates after each executable batch:

```sh
./scripts/migration/verify-route-inventory.sh
./scripts/migration/verify-contract-fixtures.sh
cargo test -p epsx-frontend
cargo test -p epsx-dioxus-ui --lib
bun test:e2e --project=frontend
```

The final A7 acceptance command from the production-readiness plan remains:

```sh
./scripts/migration/verify-no-frontend-sample-data.sh
```

That final verifier does not exist in this audit slice and must be implemented only with the route batches, after intentional static content is distinguished from executable sample business data. Integrity PASS means the audit is internally consistent; it does not mean the frontend is production ready.
