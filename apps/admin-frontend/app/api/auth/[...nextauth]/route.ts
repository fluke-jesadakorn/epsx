import { handlers } from "../../../../lib/auth"

/**
 * Auth.js v5 API route handler for admin frontend application
 * Handles all authentication routes: /api/auth/*
 */
export const { GET, POST } = handlers