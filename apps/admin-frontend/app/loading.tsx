/**
 * Global loading UI for the admin frontend
 * Shows while pages are loading
 */

import { DashboardSkeleton } from '@/components/admin/DashboardSkeleton'

export default function Loading() {
  return <DashboardSkeleton />
}