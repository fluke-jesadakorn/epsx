/**
 * ADMIN FRONTEND ALERT - RE-EXPORTS FROM SHARED
 * Backward compatibility layer
 */
export { Alert, AlertDescription, AlertTitle, alertVariants } from '@/shared/components/ui/alert';
export type { AlertDescriptionProps, AlertProps, AlertTitleProps } from '@/shared/components/ui/alert';

