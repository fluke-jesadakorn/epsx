{
  "name": "epsx-bsc-contracts",
  "version": "1.0.0",
  "description": "EPSX Payment Escrow Smart Contracts for BSC",
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "deploy:testnet": "forge script script/Deploy.s.sol --rpc-url $BSC_TESTNET_RPC --broadcast",
    "deploy:mainnet": "forge script script/Deploy.s.sol --rpc-url $BSC_MAINNET_RPC --broadcast",
    "clean": "forge clean"
  },
  "devDependencies": {},
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.3.1",
    "ethers": "^6.9.2"
  }
}
