// Environment configuration for frontend
// NOTE: Shared variables are loaded via Next.js config from /.env.shared

import { sharedFrontendEnv } from '../../../shared/config/frontend-env';

export const env = sharedFrontendEnv;