"use client";

// This component is no longer needed as auth status is handled by the onIdTokenChanged
// listener in the AuthContext provider.
export default function AuthStatusComponent() {
  return null;
}
