apiVersion: v1
kind: Service
metadata:
  name: admin-frontend-service
  namespace: epsx
  labels:
    app.kubernetes.io/name: epsx
    app.kubernetes.io/component: admin-frontend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: epsx
    app.kubernetes.io/component: admin-frontend